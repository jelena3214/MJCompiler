program Test
	//Greska zbog y = 'j'
	int xx;
	const int x = 4, y = 'j';
	{
		int s(){
			if(x == 5)return x;
		}
		
		void main()
		char p;
		int i;
		char pp[];
		{
			//greska nije dobar tip
			p++;
			p = 'j';
			// Greska continue izvan for petlje
			continue;
			
			for(i = 0; i < x; i++){
				print(i);
				if(i == 4) break;
			}
			
			read(pp);
		}
	
	}